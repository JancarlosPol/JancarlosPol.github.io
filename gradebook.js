// Stub for fetching grade data (you'll implement this later)
function fetchGradeData() {
    console.log("fetchGradeData called");
}

// Stub for populating the gradebook table
function populateGradebook() {
    console.log("populateGradebook called");
}

// Call the stubs to verify they're connected to the HTML
fetchGradeData();
populateGradebook();
